package main

import "testing"

// Phase 0 placeholder: real fixture-driven tests (banned/citizen/rate-limit/
// Castle Mail/fuzz) land in Phase 1 per PLAN.md. This keeps `go test ./...`
// non-empty for the package from day one.
func TestPlaceholder(t *testing.T) {
	t.Log("gatekeeper test suite lands in Phase 1")
}
