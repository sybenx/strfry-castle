name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: "1.26"

      - name: make test
        run: make test

      - name: make build
        run: make build

      - name: assert steward binaries are static
        run: |
          set -e
          for arch in amd64 arm64; do
            file bin/linux-$arch/steward
          done
          # ldd only runs natively on the amd64 runner; a static binary
          # makes it print this exact message rather than resolving libs.
          if ! ldd bin/linux-amd64/steward 2>&1 | grep -q "not a dynamic executable"; then
            echo "steward (linux/amd64) is not a static binary" >&2
            exit 1
          fi

      - name: make bytecheck
        run: make bytecheck
